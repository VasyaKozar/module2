<%= form_for :post, url:catalogs_path do |f| %>
<% if @product.errors.any? %>
 <% @product.errors.full_messages.each do | msg| %>
 <div class="alert alert-danger"><%= msg %></div>
<% end %>
<% end %>
<p>
	Название<br>
	<%= f.text_field :title %>
	</p>
	<p>
	Cтаття<br>
	<%= f.text_area :description %>
	</p>
	Cтаття<br>
	<%= f.text_area :price %>
	<p>
	<%= f.submit %>
	</p>
<% end %>